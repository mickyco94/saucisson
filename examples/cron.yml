services:
  - name: cron
    condition:
      type: cron
      config:
        schedule: "*/10 * * * * *"
    execute:
      type: shell
      config:
        shell: bash
        command: sleep 15; echo done
