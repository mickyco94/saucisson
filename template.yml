services:
  - name: listen_for_file_create
    condition:
      file:
        path: "/home/micky/dev/saucisson/TODO.md"
        operation: "update"
    execute:
      shell:
        command: |
          "echo file created"
