services:
  - name: do_thing
    condition:
      cron:
        schedule: "*/5 * * * * *"
    execute:
      shell:
        command: |
          "echo hello"
  - name: listen_for_file_create
    condition:
      file:
        path: "/home/micky/dev/saucisson/tests/integration/file"
        operation: "CREATE"
    execute:
      shell:
        command: |
          "echo file created"
